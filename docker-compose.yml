version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: decp-mongodb
    network_mode: bridge
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
